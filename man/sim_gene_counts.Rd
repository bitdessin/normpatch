% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_gene_counts}
\alias{sim_gene_counts}
\title{Simulate RNA-seq Gene Counts}
\usage{
sim_gene_counts(
  n_genes = 10000,
  n_replicates = c(3, 3),
  p_DEG = c(0.04, 0.01),
  fc_mean = c(4, 4),
  fc_dist = c("lognormal", "gamma", "fixed"),
  fc_sd = 0.3,
  group_names = NULL,
  seed_counts = NULL
)
}
\arguments{
\item{n_genes}{Number of genes to simulate.}

\item{n_replicates}{Numeric vector giving the number of replicates per group.}

\item{p_DEG}{Numeric vector giving the proportion of genes assigned as DEGs
for each group.}

\item{fc_mean}{Numeric vector giving the mean fold change for genes assigned
to each group. When \code{fc_dist = "fixed"}, this exact value is used.}

\item{fc_dist}{Distribution used to generate fold changes for genes assigned
as DEGs. One of \code{"lognormal"}, \code{"gamma"}, or \code{"fixed"}. The default is
\code{"lognormal"}.}

\item{fc_sd}{Spread parameter for randomized fold changes. For
\code{fc_dist = "lognormal"}, this is the standard deviation on the natural log
fold change scale. For \code{fc_dist = "gamma"}, this is the coefficient of
variation.}

\item{group_names}{Optional group names. If \code{NULL}, groups are named \code{"G1"},
\code{"G2"}, and so on.}

\item{seed_counts}{Optional seed count matrix. Rows are genes and columns
are samples. If \code{NULL}, the package dataset \code{arab} is used.}
}
\value{
A \linkS4class{SeqCountData} object. Its \code{meta} slot is a
\linkS4class{SimParams} object.
}
\description{
Generates a synthetic RNA-seq count matrix from a seed count matrix.
If no seed matrix is supplied, the \code{arab} count matrix bundled with
normpatch is used as the seed. The seed matrix is scaled to a common library
size and then used as the sampling population by computing row means and
variances. Counts are sampled from a negative binomial distribution after
applying group-level fold changes.
}
\examples{
set.seed(1)
seed <- matrix(stats::rnbinom(600, mu = 50, size = 5), nrow = 100)
x <- sim_gene_counts(n_genes = 50, seed_counts = seed)
table(def_DEG(x, fc = 4))

}
